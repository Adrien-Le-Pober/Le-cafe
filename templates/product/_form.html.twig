{{ form_start(form) }}
    {{ form_row(form.name, {
        label: 'Nom'
    }) }}
    <div class="row">
        <div class="col-6">
            {{ form_row(form.price, {
                label: 'Prix'
            }) }}
        </div>
        <div class="col-6 align-self-center">
            {{ form_row(form.stock, {
                label: 'En stock?'
            }) }}
        </div>
    </div>
    {{ form_row(form.picture, {
        label: 'Lien de l\'image'
    }) }}
    <div class="d-flex justify-content-center">
        <button class="btn btn-primary bg-dark col-6">{{ button_label|default('Créer') }}</button>
    </div>
{{ form_end(form) }}
